  
  // reports
  export const Reports = ({ type }) => (
    <div className="p-6">
      <h1 className="text-2xl font-semibold text-gray-900">{type.charAt(0).toUpperCase() + type.slice(1)} Reports</h1>
      <p className="mt-2 text-gray-600">Reports interface coming soon...</p>
    </div>
  );
  